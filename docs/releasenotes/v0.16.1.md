# pbflags v0.16.1

This patch release fixes a critical database schema issue in the sync tool.

## Sync Tool

• Fixed sync upsert operations that were incorrectly referencing a dropped database column, preventing flag definitions from being properly synchronized to the server.
