# pbflags v0.19.1

This patch release fixes a critical bug in the compilation process that prevented YAML value overrides from being properly applied.

## Server

• Fixed bundle compilation dropping YAML value overrides from the DefaultValue field, ensuring flag default values are correctly preserved during the build process.
