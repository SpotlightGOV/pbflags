# pbflags v0.22.0

This release improves operational visibility by adding configurable logging levels to server components.

## Server

- Added `--log-level` CLI flag to both evaluator and admin servers for better log output control.
