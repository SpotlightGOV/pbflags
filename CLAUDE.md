# Claude Code Instructions

At the start of every session, read `AGENTS.md` in the project you are working in (and any parent directories up to the repo root). These files contain project-specific conventions, tooling, and workflow rules that must be followed.
