// Code generated by protoc-gen-pbflags. DO NOT EDIT.
package org.spotlightgov.pbflags.generated;

import org.spotlightgov.pbflags.Dimension;
import org.spotlightgov.pbflags.FlagEvaluator;

/**
 * Feature flag accessor for the {@code user} evaluation scope.
 *
 * <p>Constructor requires all dimensions available in this scope.
 * Feature accessors are cached on first access.
 */
public final class UserFeatures {

  private final FlagEvaluator eval;
  private final String sessionId;
  private final String userId;
  private NotificationsFlags cachedNotifications;

  public UserFeatures(FlagEvaluator eval, String sessionId, String userId) {
    this.eval = eval;
    this.sessionId = sessionId;
    this.userId = userId;
  }

  /** Returns a {@link NotificationsFlags} client scoped to this evaluation context. */
  public NotificationsFlags notifications() {
    if (this.cachedNotifications == null) {
      FlagEvaluator scoped = this.eval;
      scoped = scoped.with(Dimensions.sessionId(this.sessionId));
      scoped = scoped.with(Dimensions.userId(this.userId));
      this.cachedNotifications = new NotificationsFlagsImpl(scoped);
    }
    return this.cachedNotifications;
  }

}
