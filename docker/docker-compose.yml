# Local development setup for pbflags (standalone mode).
#
# Usage:
#   docker compose -f docker/docker-compose.yml up --build
#
# Ports:
#   - 9200: Admin API + Web UI
#   - 9201: Evaluator API (HTTP/Connect)
#   - 5433: PostgreSQL
services:
  db:
    image: postgres:18-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: pbflags
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    volumes:
      - pbflags-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d pbflags"]
      interval: 5s
      timeout: 3s
      retries: 5

  pbflags:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command:
      - --standalone
      - --database=postgres://admin:admin@db:5432/pbflags?sslmode=disable
      - --descriptors=/config/descriptors.pb
      - --listen=0.0.0.0:9200
      - --evaluator-listen=0.0.0.0:9201
      - --env-name=local
    ports:
      - "9200:9200"
      - "9201:9201"
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9201/healthz || exit 1"]
      interval: 3s
      timeout: 3s
      retries: 10
    volumes:
      - ../internal/evaluator/testdata/descriptors.pb:/config/descriptors.pb:ro

volumes:
  pbflags-pgdata:
